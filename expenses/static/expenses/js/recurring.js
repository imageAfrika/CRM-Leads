document.addEventListener('DOMContentLoaded', function() {
    const recurringToggles = document.querySelectorAll('.recurring-toggle');
    
    // Recurring expense toggle handling
    recurringToggles.forEach(toggle => {
        toggle.addEventListener('change', function(e) {
            // Add toggle logic
        });
    });
    
    // Recurring schedule preview
    const updateSchedulePreview = () => {
        // Add schedule preview logic
    };
}); 